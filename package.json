{
  "name": "qr-test",
  "version": "0.1.0",
  "private": true,
  "description": "Prototype for a one-time QR vocabulary test",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
